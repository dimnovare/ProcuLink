FOUNDER APPROVED 2026-06-11: structural redesign GO, with flag-fallback plan (classic view default
until parity gates G1-G7 green + founder watches one real Triage session and signs off G8: conditional
confirm checkbox + triptych-as-submode identity). Execution = batch 9 phases A->B->C after batch 5 merges.

=== SCORES ===
Current screen (baseline): total=45 (resolve=3 clicks=3 13in=2 scale=2 kb=4 learn=4 brand=9 risk=10)
Fix Queue — guided exception triage with Context Stage: total=93 (resolve=9 clicks=9 13in=9 scale=9 kb=9 learn=8 brand=6 risk=7)
Breathing Bridge — stage-gated progressive triptych: total=86 (resolve=8 clicks=9 13in=9 scale=7 kb=8 learn=6 brand=9 risk=5)
Queue & Bench — email-client split: total=91 (resolve=9 clicks=9 13in=9 scale=9 kb=9 learn=8 brand=5 risk=6)

WINNER: Fix Queue — guided exception triage with Context Stage (93/110), narrowly over Queue & Bench (91) on brand identity (triptych stays one keystroke inside the Review tab as Full-document mode, not a separate non-default tab) and build safety (shorter build, no virtualization, both backend spikes verified ALREADY RESOLVED in code). Breathing Bridge (86) loses on the rail-geometry risk (#185 wire-bug class) and stage-auto-advance learnability. Baseline scores 45 — the winner beats it 9-vs-3 on BOTH x2 criteria, which is decisive.
VERDICT: structural-redesign

=== SIGNIFICANT IMPROVEMENT CASE ===
YES — significant, with one honest decomposition. Quantified per-order: (a) 30-AI-line order: 30 clicks + 30 reshuffling refetches today -> 2 interactions; the bulk endpoint AND apiClient.acceptAiSuggestions ALREADY EXIST (OrdersController.cs:1075-1095, api-client.ts:1296) and are simply not wired into this screen — part of the gain is shockingly cheap. (b) 10-manual-code order: ~30 interactions + 10 re-locates in a reshuffling list -> 10x type+Enter with focus auto-advance, 0 clicks, 0 hunting. (c) Send: 3-4 interactions + manual stale validation + up to 90s synchronous polling -> 2 interactions (C, Enter), validation auto-fresh, delivery backgrounded with 'Next order'. (d) Two P0 dead-ends structurally eliminated: the Reject trap (local-state strike-through, SpineReview.tsx:2582-2585, recoverable only by reload) and the invisible flagged-but-coded blocker class (err predicate :147 excludes it; blocks Send with zero affordance). At persona-1 volume (300 orders/mo, ~6-10 blockers/order) this removes roughly 4-7 operator-hours/month of pure clicking/hunting/waiting plus the reload-recovery incidents. HONEST CAVEAT: ~half the interaction savings (bulk wire-up, auto-advance, conditional confirm checkbox, auto-revalidate) are layout-independent mechanics shippable inside the current layout (Phases A-B). The STRUCTURAL queue+stage composition is additionally and independently justified by what mechanics alone cannot fix: the 60-75px horizontal overflow cutting the output column at 1280/13-inch, the 768-1279 band that today loses ALL resolution wiring, the ~200px banner stack above the fold, the per-commit list reshuffle, and the O(1)-stage scale story at 50-300 lines. Both x2 daily-life metrics improve 3->9; the founder bar ('improve daily life very significantly') is met.

=== SPEC ===
CHOSEN PATH: Fix Queue + Context Stage, with the best parts of the losers folded in. buildRisk scores above are inverted (10 = lowest risk).

== 1. EXTRACTION LIST (from SpineReview.tsx, C:/Users/Dmitri.REDACTED-PARTY/source/repos/project-proculink/src/components/bridge/SpineReview.tsx; pure refactor, zero behavior change, both views consume these) ==
HOOKS (new files under src/components/bridge/review/hooks/):
- useOrderReview(orderId): the order useQuery + 3s conditional refetchInterval (:1994-1997) + stuck-order detection (:1962) + derived exceptionCount (:2427-2429) and finalDeliveryMessage (:246-266).
- useResolveActions(orderId): handleAcceptSubnode (:2551-2580), handleCommitLineEdit (:2600-2619), handleCommitHeader (:2485-2518) — all over apiClient.commitMappings (api-client.ts:1308; backend POST /api/orders/{id}/resolve accepts a LineResolutions LIST, OrdersController.cs:426-430) + invalidate + refetch. ADD: bulkAcceptSuggestions(minConfidence) wrapping the EXISTING apiClient.acceptAiSuggestions (api-client.ts:1296 -> OrdersController.cs:1084) — no backend work. ADD: confirmFlaggedLine(line) = commitMappings(orderId, [{lineNumber, supplierItemCode: line.supplierItemCode}]) — verified to clear NeedsReview unconditionally (OrderResolutionService.cs:91). DELETE handleRejectSubnode (:2582-2585) and the rejected/accepted local Sets.
- useAcceptanceValidation(orderId): the existing validate call (:3077-3096) + validationResult/failingRuleCount (:2454-2456) + NEW effect: trailing-debounced (800ms) auto-revalidate on every commitMappings/bulk success; expose isStale flag while revalidating.
- useSendFlow(orderId): transform/deliver mutations, polling, severity flow notices (:2653-2743, :2913-2936), confirm-dialog state (:1566-1673).
COMPONENTS (extract as-is): ManualCodeRow (datalist catalog-union-mappings + not-in-catalog/novel-code warnings, :490-547); AiSuggestionContent (confidence+reason+provenance, :847-865); header inline-edit field (:679-705) with Tab-chain (:2520-2529); ConfirmDialog (focus trap, :1573-1598); DocumentAnatomy; OutputPreview + new lineFragment prop reusing the format-honesty rules (:1381-1393); StandardsFieldPopover + NODE_TO_FIELD (:270-276); SpineConnectors exported geometry fns (fallbackSourceAnchor, orderWiresForRender — used for stage mini-wires, NEVER reimplemented).
NEW PURE FUNCTION: buildFixQueue(order, validationResult, prevQueue) in src/components/bridge/review/buildFixQueue.ts — emits cards keyed by lineId/ruleId/headerKey, four types (ai-suggestion / manual-code / review-flag / rule-failure) + header-issue rows; severity-then-line order FROZEN against prevQueue (resolved cards keep index, collapse in place; newly flagged lines APPEND with an aria-live '1 new issue' pill — stolen from Queue & Bench). Unit tests: stable index on resolve; flagged-with-code lines included (closes the :147 gap); queue length === server needsReview count + failingRuleCount.

== 2. COMPOSITION PLAN ==
Review tab body becomes a two-region grid: FIX QUEUE rail (360px at 1280, 400px at >=1536) + CONTEXT STAGE (remaining width, sticky). Header/tab row/sticky bottom bar unchanged. DELETE the actionless open-exceptions panel (:3030-3061) and the standalone always-on acceptance panel (:3063-3148); sample/stuck banners compress to 28px dismissible strips (frees ~200px above the fold). Queue header: progress bar, 'Accept all >=90% (k)' (one call to the existing bulk endpoint + ONE refetch), filter chips, persistent Kbd hints. Cards bind the extracted hooks verbatim; 'Enter manually (E)' converts an AI card to ManualCodeRow (replaces Reject); ADD 'Skip — keep flagged (S)' (stolen from Queue & Bench): advances focus, card stays open with a skipped badge — never local-state-done. Review-flag cards show the flag reason + 'Confirm values' (verified path) + Edit. Context Stage: breadcrumb + DocumentAnatomy zone crop (honest 'No source snippet' fallback for API/EDI orders) + canonical line row with StandardsFieldPopover + OutputPreview fragment with unresolved token highlighted + mini-wires via the exported anchor fns + 'Acceptance rules affecting this line' strip. STOLEN FROM BREATHING BRIDGE: (a) Send-Readiness card docked above the Send CTA in the confirm flow — rows: Lines resolved / Acceptance X-of-Y (auto-fresh) / Conformance in the SUPPLIER'S configured format (fix hardcoded 'cxml', ConformancePanel.tsx:77) / 'Delivers via {real protocol}' (fix hardcoded HTTP footer :1538-1543); (b) promote-button copy fix reflecting backend batch 4A (88811b6): 'Save mappings for {supplier} — applies to their next order automatically' (replaces stale 'coming soon' :2318-2320); (c) >=44px row-level 'Change source' affordance launching the existing keyboard connect-mode (fixes 12px handle reliance). STOLEN FROM QUEUE & BENCH: &line=N / ?fix= deep link, g-d/g-b tab hotkeys + '?' shortcut overlay, supplier-name edit-time warning 'printed name only — never changes routing' (surfacing the :166-170 comment). FULL DOCUMENT MODE: today's triptych body (banners excluded) mounted byte-identical — EdgeRails, SpineConnectors, WireDragLayer, SourceWireDragLayer, SourceTokenPanel, promote, min-w-[1120px] — xl-gated as now, with sidebar auto-collapse to 66px at <1440 on entry (restore on exit). RESPONSIVE: 1280 = 360+~660 zero overflow; 1024-1279 = queue 320 + slim stage (RESTORES resolution to the band that today loses all wiring); 768-1023 = queue + disclosure stage; <768 = accordion queue, same handlers; >=1920 = stage panels side-by-side. Confirm dialog: checkbox renders ONLY when exceptionCount>0 OR failingRuleCount>0 OR validation stale; 'Send anyway' ack kept (always fresh now); on confirm, dialog closes, delivery becomes a non-blocking header pill + 'Next order' button. CONSTRAINT COMPLIANCE: no localStorage mode flag (one-experience rule) — sub-view default is deterministic: Triage iff exceptionCount>0 OR failingRuleCount>0, else Full document.

== 3. PHASES (9-12 days total; both former backend spikes verified already shipped) ==
PHASE A (3d): hook extraction + buildFixQueue + tests; FixQueue rail with 4 card types; delete dead panels; [Triage | Full document] sub-toggle routing the untouched triptych; copy fixes (promote, protocol, conformance default). Ships as a strict superset. PHASE B (3-4d): focus auto-advance + roving tabindex + aria-live progress; bulk accept wiring; Confirm values; Skip; auto-revalidation effect; conditional confirm checkbox BEHIND A CONFIG FLAG defaulting to current behavior; Next order continuation. PHASE C (3-4d): Context Stage + mini-wires; responsive compositions incl. 768-1279 repair; sidebar auto-collapse; deep links; Playwright spec surgery (live-po-loop.spec.ts + live-po-failure-states.spec.ts target the new DOM — budget 1-1.5d).

== 4. PARITY GATES (each blocks the default flip) ==
G1 Send-guard parity: CTA label/disabled derives ONLY from server exceptionCount + order.status in BOTH views; a card collapses to done ONLY after refetch confirms. G2 Single-path parity: every mutation in both views goes through commitMappings / acceptAiSuggestions; zero new endpoints; grep-gate: no setState that marks a line resolved. G3 Blocker coverage: buildFixQueue unit tests prove queue length === needsReview lines + failing rules (incl. flagged-with-code class). G4 e2e parity: both Playwright specs green against BOTH compositions (parameterized ?view=) on :5223 per the local golden-path recipe. G5 Wire-geometry freeze: SpineConnectors/WireDragLayer/SourceWireDragLayer internals untouched in A-B; geometry fn tests green; Full-document visual snapshot identical. G6 Honesty: output fragment obeys cXML-only-when-configured; confidence only from backend; errors never green; real delivery protocol shown. G7 A11y: keyboard-only full pass (type-Enter xN, then C, Enter) recorded in e2e; tab order matches visual order (ui-ux-pro-max High). G8 Founder sign-offs: conditional checkbox removal (safety ritual on the money action) and the bridge-demotion identity call — explicit approval after watching one real session.

=== GRAB FROM LOSERS ===
- Breathing Bridge: Send-Readiness card docked at the CTA (lines resolved / acceptance X-of-Y auto-fresh / conformance in the supplier's REAL configured format / real delivery protocol) — replaces three scattered verification surfaces
- Breathing Bridge: one-line copy/config fixes shippable day 1 — promote button copy reflecting batch 4A (kills stale 'coming soon', SpineReview.tsx:2318-2320), real-protocol footer (:1538-1543), ConformancePanel default format from supplier config (ConformancePanel.tsx:77)
- Breathing Bridge: >=44px row-level 'Change source' affordance launching the existing keyboard connect-mode, ending sole reliance on 12px drag dots
- Queue & Bench: ?view= / ?layout= flag-fallback rollout mechanics with the current composition mounted unchanged as the classic view — the rollback story
- Queue & Bench: 'Skip — keep flagged (S)' as the honest third action (advance without resolving, card stays open with a skipped badge) — completes the Reject-trap replacement
- Queue & Bench: newly server-flagged lines APPEND to the frozen queue with an aria-live '1 new issue' pill instead of re-sorting — prevents the invisible-blocker class reappearing in a new form
- Queue & Bench: &line=N deep link, g-d/g-b tab hotkeys, '?' shortcut overlay, and the edit-time 'printed name only — never changes routing' supplier-name warning
- Queue & Bench: 768-1023 queue-first composition extending the tablet repair below 1024 (Fix Queue originally stopped at 1024)

=== FALLBACK PLAN ===
Ship the new composition behind a ?view= URL flag (and NEXT_PUBLIC_REVIEW_TRIAGE env default) with the CLASSIC composition (today's full SpineReview body, byte-identical, sharing the extracted hooks) as the default for one release. Flip criteria, all required: (1) e2e parity — live-po-loop.spec.ts + live-po-failure-states.spec.ts green against BOTH views; (2) parity gates G1-G7 green; (3) founder watches one real multi-order session in Triage and explicitly approves the conditional-checkbox change and the triptych-as-submode identity call (G8). After the flip, classic remains reachable as the Full-document sub-mode permanently and via ?view=classic for at least one release. Rollback at any point = flag flip; no data, endpoint, or migration changes exist to undo.

=== CONCEPT SUMMARIES ===
* Fix Queue — guided exception triage with a one-line Context Stage: The screen opens on the WORK, not the document. A left-rail Fix Queue is a stable, server-truth list of every blocker (missing codes, AI suggestions, has-code review flags, failing acceptance rules, h
  flow: OPEN → FIRST FIX: order opens in Triage with the first open card's primary control already focused. Current: scan the page, locate the top needs-review line inside the center-column card stack (≈2-4s hunting + 1 click for manual lines). New: 0 clicks
  cost: Assuming the resolve/validate/transform/send hooks are already extracted and tested (stated precondition), this is a body-composition rewrite of Spine
* Breathing Bridge — stage-gated progressive triptych: The triptych IS the brand, but it currently pays for all three columns all the time — which breaks 1280px, starves the resolution workspace, and buries the send-gate. The fix: the bridge breathes. The
  flow: 12-line order: 6 AI ≥90%, 1 AI at 71%, 3 missing codes, 2 coded-but-flagged. (1) Open from inbox — 1 click, lands in RESOLVE, Fix bar says 'Issue 1 of 7' (vs today: same click, but flagged lines invisible). (2) Bulk: 'Accept all ≥90% (6)' → confirm =
  cost: ~12-16 dev-days across 4 independently shippable increments, assuming the constraint holds (resolve/validate/transform/send hooks already shared + tes
* Queue & Bench — an email-client split for line resolution (master queue left, focused line bench right, server-truth send strip on top; the existing wire triptych survives intact as the "Document" tab): SPLIT QUEUE+DETAIL. The daily job is not "look at a document," it is "burn down a list of blocked lines." So make the blocked-line list a first-class master pane (like an email inbox: j/k, auto-advanc
  flow: (1) Open order from inbox → first Needs-attention row auto-selected, bench loaded, correct control autofocused. 0 clicks, 0 scrolling (current: 0 clicks but visual hunt after each reshuffle). (2) AI-suggested line: press A → optimistic green check on
  cost: 2.5–3.5 weeks for one frontend dev, GIVEN the stated precondition that resolve/validate/transform/send hooks are already extracted and tested. Breakdo
