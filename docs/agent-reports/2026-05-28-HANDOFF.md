# Session handoff — 2026-05-28

This document briefs the next Claude session on exactly where the previous session left off and what to pick up. **Start your new session from `C:\Users\Dmitri.REDACTED-PARTY\source\repos\ProcuLink`** (NOT from the Estoria folder) so sub-agents get correct write access to both ProcuLink repos.

---

## Why the previous session was stopped

The previous session was rooted in the Estoria project folder. As a result, **sub-agents that tried to Edit/Write files inside `C:\Users\Dmitri.REDACTED-PARTY\source\repos\project-proculink\src\**` were systematically denied** by the harness sandbox. The parent thread had working write access, but sub-agents did not.

This was confirmed three times in a row:
- Polish slice 2 Agent F3 (sidebar polish) — denied
- Polish slice 2 Agent F4 (dashboard KPIs) — denied
- Polish slice 2 Agent F1 (inbox live API) — eventually 529-Overloaded (separate issue)

A session started from the ProcuLink folder should not have this problem, and sub-agents can be used freely.

---

## What is currently on disk

### `C:\Users\Dmitri.REDACTED-PARTY\source\repos\ProcuLink` — backend

**Committed and pushed to `origin/main` today:**
- `1d36240` feat(backend): idempotency + per-org AI cost cap + startup env validator
- `ae0993a` docs: trust pack + format/channel roadmap + GTM enablement + overnight audit
- `c395b6c` feat(engine): add EDIFACT + UBL/Peppol parsers, AI schema inferencer, SMTP receive
- `2bd4ecd` feat(engine): wire new parsers + services into DI + factory content-sniffing + upload whitelist
- `56918b3` docs: polish pass 16 audit — per-page list of half-wired buttons
- `58b56f2` docs: polish slice 2 shell report — drafts hidden + demo-mode badge

**Uncommitted in the working tree (these are from a SECOND parallel session running Wave 2 — DO NOT TOUCH unless you confirm with the founder that the other session is done):**

```
M ProcuLink.Infrastructure/ProcuLink.Infrastructure.csproj
M ProcuLink.Infrastructure/Services/Email/InboundEmailRouter.cs
?? NuGet.Config
?? ProcuLink.Core/Entities/ImportedS3Object.cs
?? ProcuLink.Core/Entities/ImportedSftpFile.cs
?? ProcuLink.Core/Entities/S3IngressConfig.cs
?? ProcuLink.Core/Entities/SftpIngressConfig.cs
?? ProcuLink.Core/Services/Ai/IEmailBodyOrderExtractor.cs
?? ProcuLink.Core/Services/Ingress/
?? ProcuLink.Core/Services/Ocr/
?? ProcuLink.Infrastructure.Tests/Services/Ai/OpenAiEmailBodyOrderExtractorTests.cs
?? ProcuLink.Infrastructure.Tests/Services/Ingress/
?? ProcuLink.Infrastructure.Tests/Services/Ocr/
?? ProcuLink.Infrastructure/Services/Ai/OpenAiEmailBodyOrderExtractor.cs
?? ProcuLink.Infrastructure/Services/Ingress/
?? ProcuLink.Infrastructure/Services/Ocr/
?? ProcuLink.Worker/Jobs/S3PollingJob.cs
?? ProcuLink.Worker/Jobs/SftpPollingJob.cs
?? docs/agent-reports/2026-05-28-email-body-extractor.md
?? docs/agent-reports/2026-05-28-pdf-ocr.md
?? docs/agent-reports/2026-05-28-s3-ingress.md
?? docs/agent-reports/2026-05-28-sftp-ingress.md
```

These are **Wave 2** of the format/channel roadmap: SFTP ingress, S3/R2 watch ingress, PDF OCR (Azure Document Intelligence), free-text email body NLP extractor. They were being written by a parallel session that the founder ran in another terminal. Confirm with the founder before committing them — they own that work.

### `C:\Users\Dmitri.REDACTED-PARTY\source\repos\project-proculink` — frontend

**Committed and pushed to `origin/main` today:**
- `8461526` feat(marketing): ROI calculator + drop fabricated stats + de-jargon landing copy
- `04bacce` refactor(ui): rename Bridge/Spine/Crossings/Dock jargon to plain procurement language
- `e69d621` feat(upload): accept .xml/.cxml/.edi/.txt alongside CSV/XLSX/PDF
- `77b8911` feat(ux): orphan-route redirects + onboarding sample CSV + celebration state
- `ffb9256` feat(ux): hide Drafts nav until backed, add Demo mode badge in topbar

**Uncommitted:** nothing in the frontend repo. Working tree is clean.

---

## Current test status

- Backend: `dotnet build ProcuLink.slnx --no-restore` → 0/0. `dotnet test` → **153/153 passing** as of the last clean run before Wave 2 files were dropped into the working tree. Wave 2 tests will be additive.
- Frontend: `bun run build` → passes. Pre-existing Sentry/Browserslist/ESLint warnings unchanged.

---

## Work to take over

### Polish slice 2 — three items still owed

The previous session shipped one of four polish-slice-2 fixes (the shell pass, F3). Three remain. Each is in `C:\Users\Dmitri.REDACTED-PARTY\source\repos\ProcuLink\docs\agent-reports\2026-05-28-polish-pass-16-audit.md` with severity tags. Detailed instructions follow.

#### 1. Wire `/inbox` to live orders API (F1 equivalent)

File: `C:\Users\Dmitri.REDACTED-PARTY\source\repos\project-proculink\src\components\bridge\InboxView.tsx`

- Drop the `ALL_ORDERS` hardcoded mock array. Replace with `useQuery(['orders'])` against the existing api-client helper. Match the pattern in `src/views/OrdersPage.tsx` exactly.
- Map `OrderDto` → the existing row shape (`status: CrossingStatus`, `po`, `buyer`, `supplier`, `fmt`, `ts`).
- Mock-mode preservation: when `isApiMockMode` (from `src/lib/api/api-client.ts`) is true, keep the original mock data so demo still works.
- Wire "↻ Sync" button (around line 325): `queryClient.invalidateQueries({ queryKey: ['orders'] })`.
- Wire "+ New order" button (around line 338): `router.push('/upload')` via `useRouter` from `next/navigation`.
- Wire "Re-process" bulk button (around line 311): for each selected order id, `POST /api/orders/{id}/redeliver` (endpoint exists). Promise.all with per-call try/catch, then a result notice.
- "Discard" bulk button (around line 314): add `disabled` and a `title="Discard requires the soft-delete endpoint (Group L)"` tooltip.
- Loading state: 3 skeleton rows. Error state: centred card with a Retry button calling refetch.

Verification: `bun run build` clean. Spot-check both mock mode (`NEXT_PUBLIC_USE_MOCK=true`) and live mode in the browser.

#### 2. Wire `/library/mappings` save panels (F2 equivalent)

File: `C:\Users\Dmitri.REDACTED-PARTY\source\repos\project-proculink\src\components\bridge\MappingEditor.tsx`

**CORRECTION** to the original audit's endpoint reference: the audit pointed at `PUT /api/suppliers/{id}/po-mapping`, but agent F2's investigation discovered that endpoint belongs to the **PO field-mapping engine** (Group D — used by `PoMappingEditor.tsx`, NOT `MappingEditor.tsx`).

`MappingEditor.tsx` is the **buyer↔supplier item-code translation table**, which semantically belongs to a `/api/suppliers/{id}/mappings` endpoint family. **That endpoint family is partially built** (`apiClient` already has read methods) **but the Add / Edit / Import write endpoints do NOT yet exist**.

Implication: wiring this UI requires either (a) backend work first to ship the missing endpoints, or (b) accepting that mutations will surface 404/405 errors in the red-notice row until the backend lands. Read F2's full analysis at `docs/agent-reports/2026-05-28-polish-slice2-mappings.md`.

Recommended sequence:
1. **Decide backend scope first.** Add the missing `POST/PUT/DELETE /api/suppliers/{id}/mappings` and `POST /api/suppliers/{id}/mappings/import` endpoints. Roughly mirror the `po-mapping` endpoints from Group D.
2. Then wire the UI per the same pattern as `PoMappingEditor.tsx`.
3. Export was planned as a client-side CSV blob — no backend dependency for that one.
- Mock-mode preservation: when `isApiMockMode` is true, keep local-only behaviour.

Verification: `bun run build` clean.

#### 3. Wire `/bridge` dashboard KPIs (F4 equivalent)

File: `C:\Users\Dmitri.REDACTED-PARTY\source\repos\project-proculink\src\components\bridge\BridgeDashboard.tsx`

The component already imports `useQuery` and has the orders query wired. The KPIs constant at lines ~49-55 is still hardcoded mock values.

**Important finding from the previous agent attempt:** `OrderSummary` (returned by `GET /api/orders`) does **not** include `updatedAt` — only `createdAt`. So "Avg processing time" cannot be computed cleanly today. Options:

- Show `—` with a TODO and declare the future-need (either extend `OrderSummary` with `updatedAt`, or build the dedicated `/api/dashboard/kpis` aggregation endpoint).
- Skip the avg-time KPI entirely and replace it with another computable one ("Failed in last 24h").

Compute the others from the live `OrderDto[]`:
- **Orders today** — count of orders with `createdAt` in the current UTC day. Format with `toLocaleString()`.
- **Urgent exceptions** — count of orders with status `pending_review` OR `delivery_failed`.
- **Auto-processed** — percentage of orders with status in (`ready`, `ready_to_deliver`, `delivering`, `delivered`) where no line had `needsReview === true`. Show `—` if data thin (<3 orders).
- **Cost per order** — leave as `—` for now (needs OpenAI usage data + delivery cost data we don't aggregate yet). Update sub-line to "Coming with usage metering".

Mock-mode preservation: when `isApiMockMode === true`, keep the existing mock numbers.

Loading state: skeleton cards (~80px tall, animated shimmer). Error state: keep the layout, show `—` in each tile, sub "Live KPIs unavailable".

Sub-line deltas vs previous period: too expensive to compute cheaply on the list endpoint. Replace each `sub` with a static current-period anchor (e.g., "Today, UTC") and add `// TODO: wire to /api/dashboard/kpis when available`.

Verification: `bun run build` clean.

---

## What's next, after polish slice 2

Order of priority. The previous session gave the founder this list — keep it.

1. **Wave 2 integration**. The Wave 2 files listed under "Uncommitted in the working tree" are the parallel session's outputs. **The parallel session has already been modifying `ProcuLink.Api/Program.cs` and `ProcuLink.Worker/Program.cs` to wire up its own Wave 2 services** (SFTP/S3 ingress, OCR, email body extractor) — so most DI integration is being done by them, not waiting for you. Your job after that session lands is mostly: pull, run `dotnet build` + `dotnet test`, fix any integration gaps (e.g., `OrderParserFactory` if it needs new file-detection branches for additional formats), update upload-whitelist if needed, then commit/push together.

2. **Group J live deploy**. Needs founder action — set Railway env vars per `STATUS.md` Group J section. Then run the 11-item e2e QA checklist on the deployed stack.

3. **Polish slice 3 — backend-dependent UI fixes**. Wire `/library/buyers` (needs new `GET /api/buyers`), `/operations/log` (needs aggregated `/api/audit`), `/library/rules` and `/library/templates` (need new APIs). Each requires backend endpoint first; the audit doc enumerates them.

4. **Wave 3 — Invoice + ASN canonical models**. Mirror the PO pipeline for invoices (UBL Invoice + EDIFACT INVOIC + canonical Invoice entity) and advance shipping notices (DESADV).

5. **Wave 4 — Zapier + Make.com connectors**. Distribution play; ~1 week each including platform review.

6. **Group L — launch readiness**. First-supplier onboarding wizard, in-app help, public changelog, status page (UptimeRobot).

**Deferred indefinitely:** AS2 (cert + 4 weeks of handshake work — defer until 3+ customers ask), PEPPOL access point accreditation (8–12 weeks + ongoing compliance — defer until €5k+ MRR).

---

## Reading order for the new session

To get up to speed in 10 minutes:

1. This file
2. `STATUS.md` — current phase + group status
3. `docs/agent-reports/2026-05-28-polish-pass-16-audit.md` — what's still half-wired
4. `docs/agent-reports/2026-05-28-overnight-summary.md` — what landed overnight
5. `docs/format-channel-roadmap.md` — the long-form engine roadmap

Then:

```bash
git -C "C:/Users/Dmitri.REDACTED-PARTY/source/repos/ProcuLink" pull --rebase origin main
git -C "C:/Users/Dmitri.REDACTED-PARTY/source/repos/project-proculink" pull --rebase origin main
```

…and pick the polish slice 2 item the founder wants next. The three remaining items are independent of each other, so you can do them in any order.

## Don't surprise the founder

- **Do not commit the Wave 2 files** (S3 ingress, SFTP ingress, OCR, email body extractor) without explicit go-ahead. They were written by a parallel session and belong to a separate atomic commit set.
- **Do not push without verifying both `dotnet build` and `bun run build` are clean.**
- **Do not introduce new npm or NuGet packages without justification.**
- **Do not touch route slugs (`/bridge`, etc.) or component file names** — internal vocabulary that was deliberately preserved (renaming requires Clerk middleware + analytics-event updates).
- **Frontend write access works in the parent thread; sub-agents on `project-proculink/src/**` may be Write-denied — the previous session hit this three times.** If you're on a session rooted at ProcuLink, sub-agents should work; if not, fall back to parent-thread execution as the previous session did for the shell polish.
